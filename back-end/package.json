{
  "name": "back-end",
  "version": "1.0.4",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "start": "npm run build && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^16.4.13",
    "ts-node": "^10.0.0",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "sitka": "^1.0.5"
  }
}
